// examples/demo.ltl — runnable demo for ltl_stack_vm

import std::io
use ltl_stack_vm as lib

fn main() {
    io::println("ltl_stack_vm demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}