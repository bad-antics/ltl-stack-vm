// ltl-stack-vm/main.ltl
//
// Minimal stack VM with JIT-ready bytecode format.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-stack-vm v0.1.0")
    io::println("Minimal stack VM with JIT-ready bytecode format.")
    0
}
